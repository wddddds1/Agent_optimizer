version: 1
default:
  families:
    parallel_omp:
      threads:
        mode: "powers_of_two"
        min: 1
        max: "physical"
        include_physical: true
        include_logical: false
        include_core_groups: true
        include_socket_cores: true
        include_numa_cores: true
        max_pool: 6
        expand_if_best_at_max: true
        expansion_step: 4
cases:
  melt_xxlarge:
    families:
      parallel_omp:
        threads:
          mode: "powers_of_two"
          min: 1
          max: "logical"
          include_physical: true
          include_logical: true
          include_core_groups: true
          include_socket_cores: true
          include_numa_cores: true
          max_pool: 6
          keep_bottom_k: 1
          keep_top_k: 2
          expand_if_best_at_max: true
          expansion_step: 4
          template_ids: ["close_cores"]
